# Private, single-user instance — not meant to be crawled or indexed at all.
# (Basic Auth in front of it already blocks anonymous crawlers in practice;
# this is belt-and-suspenders, and matches <meta name="robots"> in index.html.)
User-agent: *
Disallow: /
